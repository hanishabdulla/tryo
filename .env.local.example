# Read by the Convex CLI (`npm run convex:dev` / `convex:deploy`) at the repo root.
# Each app has its own .env.local for NEXT_PUBLIC_CONVEX_URL — all must point at
# the same deployment.
CONVEX_DEPLOYMENT=dev:your-deployment-name
